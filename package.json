{
  "name": "doineedmybrolly",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n worker,frontend -c blue,green \"npm run dev:worker\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:worker": "npm run dev --prefix worker",
    "build": "npm run build:frontend",
    "build:frontend": "npm run build --prefix frontend",
    "typecheck": "npm run typecheck:frontend && npm run typecheck:worker",
    "typecheck:frontend": "npm run typecheck --prefix frontend",
    "typecheck:worker": "npm run typecheck --prefix worker",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix worker && npm install --prefix scripts",
    "generate:locations": "npm run generate-locations --prefix scripts",
    "generate:sitemap": "npm run generate-sitemap --prefix scripts"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
