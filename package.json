{
  "name": "doineedmybrolly",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "typecheck": "npm run typecheck --prefix frontend && npm run typecheck:functions --prefix frontend",
    "install:all": "npm install --prefix frontend && npm install --prefix scripts",
    "generate:locations": "npm run generate-locations --prefix scripts",
    "generate:sitemap": "npm run generate-sitemap --prefix scripts"
  }
}
